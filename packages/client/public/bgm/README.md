# BGM 目录

存放 BGM 文件。`main.mp3` 用于主界面 / 选人界面，角色 BGM 用于对战时随机播放。

## 命名规则

文件名必须与角色 id 一致，扩展名支持 `.mp3`、`.ogg`、`.wav`（优先尝试 `.mp3`）。

示例：

```
public/bgm/
  youmu.mp3
  reimu.mp3
  marisa.mp3
  ...
```

## 播放规则

开始对战时会从对战双方角色中随机选择一人，播放其对应 BGM。
若找不到对应文件或播放失败，则静默跳过，不会影响游戏进行。
